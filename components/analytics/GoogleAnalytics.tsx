'use client'

/**
 * Deprecated: Google tags (GA4 + Ads) bootstrap in app/layout.tsx <head>
 * so Google Ads Tag Assistant / website scanners can detect AW-….
 * Consent Mode is updated via CookieConsentBanner + ConsentAwareAnalytics.
 */
export default function GoogleAnalytics() {
  return null
}
